import { PaintingListAdapter } from './painting-list-adapter';

describe('PaintingListAdapter', () => {
  it('should create an instance', () => {
    expect(new PaintingListAdapter()).toBeTruthy();
  });
});
