import { PaintingDetailsAdapter } from './painting-details-adapter';

describe('PaintingDetailsAdapter', () => {
  it('should create an instance', () => {
    expect(new PaintingDetailsAdapter()).toBeTruthy();
  });
});
