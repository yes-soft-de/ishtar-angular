export interface LoveRequest {
  // Entity Type
  entity: number;
  // Entity Id
  row: number;
  // Interaction Type
  interaction: number;
  // Client Id
  client: number;
  // Interaction Type
  interactions?: number;
}
